apiVersion: v1
kind: Pod
metadata:
  name: memory-stress
spec:
  containers:
  - name: stress
    image: busybox
    command: ["sh", "-c", "x=1; while true; do x=$x$x$x$x; done"]
    resources:
      requests:
        memory: "512Mi"
      limits:
        memory: "1Gi"
